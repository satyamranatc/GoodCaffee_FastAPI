from pydb import connect

# Configure your database connection here
# Replace placeholders with actual credentials before running
connect(
    host="localhost",
    user="root",
    password="satyamrana",
    database="goodcaffee_db"
)
